$schema: "http://json-schema.org/draft-06/schema#"

description: sample sheet

type: object

properties:
  
  sample:
    type: string
  
  condition:
    type: string
  
  batch:
    type: string
  
  block:
    type: string
  
  filename:
    type: string

required:
  - sample
  - condition
  - filename
